<div>
    <h1>Listado de productos</h1>
    <ul></ul>
    {{#each products}}
        <li>{{this.title}}</li>
        <li>{{this.description}}</li>
        <li><img href={{this.thumnail}}></img></li>
    {{/each}}
</div>